# Examples

This directory contains runnable scripts that demonstrate how to use OpenClinical AI.

## Available Examples

| File | Description |
|------|-------------|
| `quickstart_inference.py` | Run clinical inference using the FastAPI runtime — start the server, authenticate, and submit a patient note for processing |

## Prerequisites

```bash
pip install -e ..
```

Each script can be run independently after installing the package.
